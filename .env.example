# Optional. Same Redis as on-demand-goose-execution (database.redis.uri).
# When unset, MCP sessions start without publishing tool progress to redis_channel.
REDIS_URL=redis://host.docker.internal:6379
